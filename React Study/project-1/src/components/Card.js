import Photo from './Photo';
import Main from './Main';
import Footer from './Footer';

export default function Card() {
    return (
        <div className="card">
            < Photo />
            < Main />
            < Footer />
        </div>
    );
}
